  apiVersion: monitoring.coreos.com/v1
  kind: ServiceMonitor
  metadata:
    name: find-moj-data-service
  spec:
    selector:
      matchLabels:
        app: find-moj-data
    endpoints:
    - port: http
      interval: 15s
